import * as d from '../../declarations';
export declare function bundleJson(config: d.Config): {
    name: string;
    resolveId(importee: string, importer: string): any;
    transform(json: string, id: string): {
        ast: ASTNode;
        code: string;
        map: {
            mappings: string;
        };
    };
};
interface ASTNode {
    type: string;
    sourceType?: string;
    start: number | null;
    end: number | null;
    body?: any[];
    declaration?: any;
}
export {};
